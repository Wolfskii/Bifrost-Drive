# Nextcloud

Nextcloud is a dedicated connection type, **Planned**, backed by the WebDAV adapter. The UI will accept a server URL, username, and app password, test the connection, and discover the WebDAV endpoint where practical. App passwords are preferred over account passwords.
