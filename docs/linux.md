# Linux

Linux support is **Planned**. The future adapter will use FUSE, desktop notifications, and Secret Service/libsecret where available. Mount permissions, file watching, cache paths, and disconnect behavior must be designed and tested as Linux-native concerns.

No Linux filesystem mount support is claimed by the current build.
